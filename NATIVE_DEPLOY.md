# 🚀 Запуск системы на виртуальной машине

## Первый раз (настройка)

### Шаг 1: Создать окружение
```bash
make setup
```

Создаст `/opt/sirius/` со структурой:
```
/opt/sirius/
├── bin/              ← Бинарники (пусто, заполнится при deploy)
├── config/           ← Токены и настройки
├── logs/             ← Логи
├── static/           ← Веб-интерфейс
├── migrations/       ← SQL миграции
└── production.yaml   ← Конфигурация
```

### Шаг 2: Настроить PostgreSQL
```bash
nano /opt/sirius/production.yaml
```

Замените строку подключения:
```yaml
storage:
  connection_string: "postgres://USER:PASSWORD@HOST:6432/DATABASE?sslmode=require"
```

### Шаг 3: Создать токены

Получите токены от @BotFather в Telegram, затем:

```bash
nano /opt/sirius/config/token.txt           # Токен пользовательского бота
nano /opt/sirius/config/admin_token.txt     # Токен административного бота
nano /opt/sirius/config/api_token.txt       # API токен (можно оставить по умолчанию)
nano /opt/sirius/config/admins.json         # Ваш Telegram ID
```

Пример `admins.json`:
```json
{"admins":[{"id":123456789,"name":"Admin","is_active":true}]}
```

### Шаг 4: Запустить
```bash
make deploy         # Соберёт, установит и запустит всё
make enable         # Включит автозапуск при перезагрузке
```

---

## Ежедневная работа

### Обновление кода
```bash
git pull
make deploy         # Одна команда: собрать + установить + перезапустить
```

### Управление
```bash
make restart        # Перезапустить всё
make stop           # Остановить всё
make start          # Запустить всё
make status         # Показать статус
```

### Логи
```bash
make logs           # Все логи в реальном времени
make logs-server    # Только сервер
make logs-userbot   # Только userbot
make logs-adminbot  # Только adminbot
```

### Все команды
```bash
make help           # Показать все доступные команды
```

---

## Команды по шагам

Если нужен контроль на каждом этапе:

```bash
make build          # 1. Собрать бинарники
make install        # 2. Скопировать в /opt/sirius
make restart        # 3. Перезапустить сервисы
```

---

## Для второго разработчика

Второй разработчик просто клонирует код и запускает:

```bash
git clone REPO_URL ~/sirius-loyality-system
cd ~/sirius-loyality-system

# Деплой (всё через sudo, конфликтов не будет)
make deploy
```

---

## Локальная разработка

Для тестирования на своей машине:

```bash
nano local.yaml                     # Настроить
nano local-config/token.txt         # Токены
make run-server                     # Запустить локально
```

Подробнее в [CHEATSHEET.md](CHEATSHEET.md)

---

## Доступ к системе

- **Веб-интерфейс:** `http://YOUR_SERVER_IP:8080/admin/`
- **API:** `http://YOUR_SERVER_IP:8080/`
- **Логи:** `make logs`

