# Пример файла окружения для sirius-loyality-system
# Скопируйте этот файл в .env и заполните реальными значениями
# НИКОГДА не коммитьте .env в репозиторий!

# ============================================
# PostgreSQL (Yandex Managed PostgreSQL)
# ============================================
# Формат: postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
#
# Где взять параметры:
#   USER     - имя пользователя БД (создаётся в консоли YC -> Managed PostgreSQL -> Пользователи)
#   PASSWORD - пароль пользователя БД
#   HOST     - FQDN хоста (консоль YC -> Managed PostgreSQL -> Хосты -> колонка "Имя хоста")
#   PORT     - 6432 (через pooler) или 5432 (напрямую)
#   DATABASE - имя базы данных (создаётся в консоли YC -> Managed PostgreSQL -> Базы данных)
#
# Пример:
# DB_CONNECTION_STRING=postgres://loyality_user:MySecretPassword@rc1a-abc123def456.mdb.yandexcloud.net:6432/loyality_system?sslmode=require

DB_CONNECTION_STRING=postgres://USER:PASSWORD@HOST:6432/DATABASE?sslmode=require

# ============================================
# Тип хранилища (опционально, по умолчанию postgres)
# ============================================
# STORAGE_TYPE=postgres
